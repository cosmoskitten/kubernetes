kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: allow-from-red-to-blue
spec:
  podSelector:
    matchLabels:
      color: blue
  ingress:
  - from:
    - podSelector:
        matchLabels:
          color: red
