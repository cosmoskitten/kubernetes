apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: nginx
  namespace: my-namespace
  labels:
    name: nginx-undo
spec:
  replicas: 3
  selector:
    matchLabels:
      name: nginx-undo
  template:
    metadata:
      labels:
        name: nginx-undo
    spec:
      containers:
      - name: nginx
        image: gcr.io/google-containers/pause:2.0
        ports:
        - containerPort: 80
